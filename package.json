{
  "name": "nmapaye-portfolio",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:sites": "npm run build && node scripts/stage-sites-build.mjs",
    "test": "npm run build && node --test scripts/test-seo.mjs scripts/test-homepage.mjs scripts/test-hosting.mjs",
    "test:sites": "npm run build:sites && node --test scripts/test-sites-build.mjs",
    "test:homepage": "npm run build && node --test scripts/test-homepage.mjs",
    "test:seo": "npm run build && node --test scripts/test-seo.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.2.1",
    "@fontsource-variable/space-grotesk": "5.3.0",
    "astro": "^4.16.18"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
